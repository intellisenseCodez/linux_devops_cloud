# Task

1. Create a Bash script named variables.sh. Define the following variables:
    - STUDENT_NAME
    - COURSE
    - BOOTCAMP_DURATION (in weeks)
    
    Assign appropriate values to each variable. Display all variable values on the terminal. 

2. Create two variables:
    - TOTAL_DAYS
    - DAYS_PER_WEEK
    
    Calculate the number of weeks using Bash arithmetic.
    
    Store the result in a variable called TOTAL_WEEKS. 
    
    Print the result in a readable sentence.

3. Write a Bash script named extract_columns.sh that performs the following:

    - Downloads a CSV file from the URL below and saves it locally as data.csv

        https://raw.githubusercontent.com/mwaskom/seaborn-data/master/iris.csv

    - Extracts only the following columns from the CSV file: sepal_length,sepal_width, species

    - Saves the extracted columns into a new file called selected_columns.csv.